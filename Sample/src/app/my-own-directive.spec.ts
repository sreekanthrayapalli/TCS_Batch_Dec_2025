import { MyOwnDirective } from './my-own-directive';

describe('MyOwnDirective', () => {
  it('should create an instance', () => {
    const directive = new MyOwnDirective();
    expect(directive).toBeTruthy();
  });
});
